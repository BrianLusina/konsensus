[tool.poetry]
name = "konsensus"
version = "0.1.0"
description = "Simple clustering by consensus library"
authors = ["BrianLusina <12752833+BrianLusina@users.noreply.github.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"
gevent = "^22.10.2"

[tool.poetry.dev-dependencies]
pylint = "^2.17.0"
pytest = "^7.2.2"
black = "^23.1.0"
pre-commit = "^3.2.0"
python-dotenv = "^1.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
